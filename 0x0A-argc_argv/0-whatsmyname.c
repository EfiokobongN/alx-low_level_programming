#include <stdio.h>
/**
 * main -  write name
 * @argc: arguement make count
 * @argv: array of pointers to the arguement strings
 * Return: 0
 **/
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
